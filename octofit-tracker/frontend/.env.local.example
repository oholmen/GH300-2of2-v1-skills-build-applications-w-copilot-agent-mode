# Example .env.local
# Define your Codespaces name when running inside Codespaces so the frontend can
# construct a Codespaces-hosted API URL. If you don't set this, the app will
# fall back to http://localhost:8000 so you can run locally.

VITE_CODESPACE_NAME=your-codespace-name-here
